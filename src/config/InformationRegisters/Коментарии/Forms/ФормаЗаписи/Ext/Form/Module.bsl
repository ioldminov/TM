
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	//параметры
	
	Если Параметры.Свойство("ОткрытаДляРедактирования") = Истина Тогда 
		ЭтаФорма.Элементы.Объект.ТолькоПросмотр = Истина;
		ЭтаФорма.Элементы.ID.ТолькоПросмотр = Истина;
		ЭтаФорма.Элементы.Пользователь.ТолькоПросмотр = Истина;
		ЭтаФорма.Элементы.ДатаДобавления.ТолькоПросмотр = Истина;
	КонецЕсли;
	
КонецПроцедуры
