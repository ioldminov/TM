
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ТекстHTML") Тогда 
	  //Элементы.ПолеHTML.Документ.Body.innerHTML = Параметры.ТекстHTML;
		ПолеHTML = Параметры.ТекстHTML;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	
	
КонецПроцедуры
