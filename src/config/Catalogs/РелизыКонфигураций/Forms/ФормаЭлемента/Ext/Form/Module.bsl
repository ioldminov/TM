
&НаКлиенте
Процедура ВыпущенПриИзменении(Элемент)
	ВыпущенПриИзмененииНаСервере();
КонецПроцедуры

&НаСервере
Процедура ВыпущенПриИзмененииНаСервере()
	
	Если Не ЗначениеЗаполнено(Объект.ДатаВыпуска) Тогда 
		
		Объект.ДатаВыпуска = ТекущаяДата();
		
	КонецЕсли;
	
КонецПроцедуры
